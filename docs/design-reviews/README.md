# Design Reviews

Durable output from the `design` agent's review mode (Live Environment First): triaged
findings with screenshot references across breakpoints, checked against design-system
consistency.
